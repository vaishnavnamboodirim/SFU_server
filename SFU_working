function onConnected() {
  isRunning = true;

  if (null != streamHandler) {
    type = streamHandler.type;
    streamHandler.viewer.fitView(view.offsetWidth, view.offsetHeight);
    document.getElementById("label-title").innerText = streamHandler.target;
  }

  subscribeEvents();
  updateMode(settings.mode);
}
